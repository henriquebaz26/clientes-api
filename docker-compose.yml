services:
  postgres:
    image: postgres:15
    container_name: postgres-clientes
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: senha
      POSTGRES_DB: clientesdb
    ports:
      - "5432:5432"